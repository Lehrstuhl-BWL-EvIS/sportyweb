<.header>
    Mitarbeiter zu <%= @club.name %> hinzufügen
    <:subtitle>Verwalter: <%= @current_user.email %></:subtitle>
</.header>
  
<.card>
    <.table id="users" rows={@users} row_click={&JS.navigate(~p"/clubs/#{@club.id}/roles/#{&1}/edit")}>
      <:col :let={user} label="Für Sie einsehbare Nutzer"><%= user.email %></:col>
      <:action :let={user}>
        <.link navigate={~p"/clubs/#{@club.id}/roles/#{user.id}/edit"}>Hinzufügen</.link>
      </:action>
    </.table>
</.card>
  
<.back navigate={~p"/clubs/#{@club.id}/roles"}>Zurück zur Nutzer & Rollen</.back>
  